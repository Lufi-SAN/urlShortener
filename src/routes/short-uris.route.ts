import { Router } from "express";

const shortURISRoute = Router();

export default shortURISRoute;

// POST   /v1/short-urls
// GET    /v1/short-urls/:id
// PATCH  /v1/short-urls/:id
// DELETE /v1/short-urls/:id
// GET    /v1/short-urls   (list)
